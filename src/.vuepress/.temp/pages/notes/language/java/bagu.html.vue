<template><div><h2 id="what-are-the-basic-principals-of-oop" tabindex="-1"><a class="header-anchor" href="#what-are-the-basic-principals-of-oop"><span>What are the basic principals of OOP?</span></a></h2>
<p>The four main OOP principles are abstraction, encapsulation, inheritance, and polymorphism.</p>
<p>Abstraction means for the class design you show only the essential features and hide unnecessary details. For example, the Student class should only include information like student number and scores etc., not the weight.</p>
<p>Encapsulation means you keep data private inside a class and expose it through public methods.</p>
<p>Inheritance means one class can reuse and extend another class's behavior.</p>
<p>Polymorphism means the same method of the parent class can behave differently in the subclasses. For example, a Vehicle class can be extended by Car and Bike, and each of them can implement drive() differently.</p>
<h2 id="what-happened-when-you-create-an-array-object-in-java" tabindex="-1"><a class="header-anchor" href="#what-happened-when-you-create-an-array-object-in-java"><span>What happened when you create an array object in Java?</span></a></h2>
<p>If we want to create an array object in Java, first we have to call the constructor. It will assign a continuous memory on heap, which stores references to the element objects. And then it returns a reference to the actual array object on heap, and that reference is stored on stack.</p>
</div></template>


