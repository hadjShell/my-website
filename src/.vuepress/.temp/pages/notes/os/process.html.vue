<template><div><h2 id="the-abstraction-a-process" tabindex="-1"><a class="header-anchor" href="#the-abstraction-a-process"><span>The Abstraction: A Process</span></a></h2>
<p>The OS creates the illusion by virtualizing the CPU. By <em>running one process, then stopping it and running another</em>, and so forth, the OS can promote the illusion that many virtual CPUs exist when in fact there is only one physical CPU (or a few). This technique, known as <strong>time sharing</strong> of the CPU, allows users to run as many concurrent processes as they would like; the potential cost is performance, as each will run more slowly if the CPU must be shared.</p>
<p>Each <strong>running program</strong>, called a <em>process</em>, has its <strong>machine state</strong>, what a program can read or update when it is running. One component of a process's machine state is <strong>user-space memory</strong> containing instructions, data, and a stack. The instructions implement the program's computation. The data are the variables on which the computation acts. The stack organizes the program's procedure calls. Another part are registers. Finally, programs often access persistent storage devices.</p>
<p>The kernel associates a process identifier, or <code v-pre>PID</code>, with each process.</p>
<h2 id="process-api" tabindex="-1"><a class="header-anchor" href="#process-api"><span>Process API</span></a></h2>
<ul>
<li><strong>Create</strong>: An operating system must include some method to create new processes. When you type a command into the shell, or double-click on an application icon, the OS is invoked to create a new process to run the program you have indicated.</li>
<li><strong>Destroy</strong>: As there is an interface for process creation, systems also provide an interface to destroy processes forcefully. Of course, many processes will run and just exit by themselves when complete; when they don’t, however, the user may wish to kill them, and thus an interface to halt a runaway process is quite useful.</li>
<li><strong>Wait</strong>: Sometimes it is useful to wait for a process to stop running; thus some kind of waiting interface is often provided.</li>
<li>Miscellaneous Control: Other than killing or waiting for a process, there are sometimes other controls that are possible. For example, most operating systems provide some kind of method to <strong>suspend</strong> a process (stop it from running for a while) and then <strong>resume</strong> it (continue it running).</li>
<li><strong>Status</strong>: There are usually interfaces to get some status information about a process as well, such as how long it has run for, or what state it is in.</li>
</ul>
<h2 id="process-creation" tabindex="-1"><a class="header-anchor" href="#process-creation"><span>Process Creation</span></a></h2>
<p>The first thing that the OS must do to run a program is to <strong>load</strong> its code and any static data (e.g., initialized variables) into memory, into the <strong>address space</strong> of the process. Programs initially reside on disk in some kind of executable format; thus, the process of loading a program and static data into memory requires the OS to read those bytes from disk and place them in memory somewhere.</p>
<p>Once the code and static data are loaded into memory, there are a few other things the OS needs to do before running the process. Some memory must be allocated for the program’s <strong>run-time stack</strong> (or just stack).C programs use the stack for local variables, function parameters, and return addresses; the OS allocates this memory and gives it to the process. The OS will also likely initialize the stack with arguments; specifically, it will fill in the parameters to the <code v-pre>main()</code> function, i.e., <code v-pre>argc</code> and the <code v-pre>argv</code> array.</p>
<p>The OS may also allocate some memory for the program’s <strong>heap</strong>. In C programs, the heap is used for explicitly requested dynamically-allocated data; programs request such space by calling <code v-pre>malloc()</code> and free it explicitly by calling <code v-pre>free()</code>. The heap is needed for data structures such as linked lists, hash tables, trees, and other interesting data structures. The heap will be small at first; as the program runs, and requests more memory via the <code v-pre>malloc()</code> library API, the OS may get involved and allocate more memory to the process to help satisfy such calls.</p>
<p>The OS will also do some other initialization tasks, particularly as related to input/output (I/O). For example, in UNIX systems, <strong>each process by default has three open file descriptors, for standard input, output, and error</strong>; these descriptors let programs easily read input from the terminal as well as print output to the screen.</p>
<p>By loading the code and static data into memory, by creating and initializing a stack, and by doing other work as related to I/O setup, the OS has now (finally) set the stage for program execution. It thus has one last task: to start the program running at the entry point, namely <code v-pre>main()</code>. By jumping to the <code v-pre>main()</code> routine, the OS transfers control of the CPU to the newly-created process, and thus the program begins its execution.</p>
<h2 id="process-state" tabindex="-1"><a class="header-anchor" href="#process-state"><span>Process State</span></a></h2>
<ul>
<li><strong>Unused</strong>: The process slot is completely free and not being used by any process.</li>
<li><strong>EMBRYO</strong> (胚胎): The state when the process is being created, but not fully initialized yet. The OS has allocated a process slot and a PID has been assigned, but memory is not fully set up yet.</li>
<li><strong>Ready</strong>: In the ready state, a process is ready to run but for some reason the OS has chosen not to run it at this given moment.</li>
<li><strong>Running</strong>: In the running state, a process is running on a processor. This means it is executing instructions.</li>
<li><strong>Blocked</strong>: In the blocked state, a process has performed some kind of operation that makes it not ready to run until some other event takes place. A common example: when a process initiates an I/O request to a disk, it becomes blocked and thus some other process can use the processor.</li>
<li><strong>Zombie</strong>: A process has exited but has not yet been cleaned up. This final state can be useful as it allows other processes (usually the <strong>parent process</strong>) to examine the return code of the process abd see if the just-finished process executed successfully.</li>
</ul>
</div></template>


