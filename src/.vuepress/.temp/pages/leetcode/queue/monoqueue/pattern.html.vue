<template><div><h2 id="🧠-concept" tabindex="-1"><a class="header-anchor" href="#🧠-concept"><span>🧠 Concept</span></a></h2>
<ul>
<li>
<p>Problem</p>
<ul>
<li>
<p><strong>给你一个数组 <code v-pre>window</code>，已知其最值为 <code v-pre>A</code>，如果给 <code v-pre>window</code> 中添加一个数 <code v-pre>B</code>，那么比较一下 <code v-pre>A</code> 和 <code v-pre>B</code> 就可以立即算出新的最值；但如果要从 <code v-pre>window</code> 数组中减少一个数，就不能直接得到最值了，因为如果减少的这个数恰好是 <code v-pre>A</code>，就需要遍历 <code v-pre>window</code> 中的所有元素重新寻找新的最值</strong>。</p>
</li>
<li>
<p>Find <strong>max/min</strong> of an array where exists <strong>frequent first or last element removal</strong></p>
</li>
</ul>
</li>
</ul>
</div></template>


